const ButtonSubmit = {
  px: 5,
  borderRadius: 5,
  color: 'white',
};

const formGrid = {
  padding: 9,
  textAlign: 'center',
};

const LeftSide = {
  borderRadius: '20px 0 0 20px',
  backgroundColor: 'primary.main',
};

const Link = {
  textDecoration: 'none',
  color: 'primary.main',
  ':hover': { textDecoration: 'underline', color: 'primary.dark' },
};

const styles = { ButtonSubmit, formGrid, LeftSide, Link };

export default styles;
